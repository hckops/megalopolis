apiVersion: v1
kind: Service
metadata:
  name: {{ include "box.fullname" . }}
  labels:
    {{- include "box.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "box.selectorLabels" . | nindent 4 }}

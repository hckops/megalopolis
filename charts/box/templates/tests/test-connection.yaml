apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "box.fullname" . }}-test-connection"
  labels:
    {{- include "box.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "box.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

kind: task/v1
name: Trivy
tags:
  - scanner
image:
  repository: aquasec/trivy
  version: latest
commands:
  - name: help
    arguments:
      - "--help"
  - name: version
    arguments:
      - "--version"
  - name: default
    arguments:
      - "image ${image:python:3.4-alpine}"
